{
  "name": "ouvidoria-digital",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm-run-all -p dev:api dev:web",
    "dev:api": "npm run dev --workspace @ouvidoria/api",
    "dev:web": "npm run dev --workspace @ouvidoria/web",
    "test": "npm run test --workspace @ouvidoria/api"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
